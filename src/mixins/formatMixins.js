export const formattingfMixins = {
    filters: {
        reverse(value) {
            return value.split('').reverse().join('');
        }
    },
    created() {
    }
};

export const someOtherMixins = {

};